Test suite scaffold for Coyote3.

- `tests/api`: FastAPI endpoint and service tests.
- `tests/web`: Flask/Jinja presentation and integration tests.

`tests_internal` is treated as legacy and is intentionally excluded from pytest discovery.
